# 一、这次 interaction 诊断补得非常好

你现在不再直接解释不可辨识的 CfkC_{fk}，而是从最终 effective operators：

TfkT_{fk}

定义：

Ifk=Tfk−Tˉf⋅−Tˉ⋅k+Tˉ⋅⋅\boxed{ I_{fk} = T_{fk} - \bar T_{f\cdot} - \bar T_{\cdot k} + \bar T_{\cdot\cdot} }

这是正确方向。

代码也确实是从 checkpoint 重建 effective TfkT_{fk}，然后 double-center，而不是依赖原始 A/B/CA/B/C 分解；因此它对：

Af↔Bk↔CfkA_f\leftrightarrow B_k\leftrightarrow C_{fk}

之间的吸收不敏感。

OADD：

Ifk=0I_{fk}=0

在全部数据上精确成立，是很好的 sanity check。报告中的结果：

- ele-fashion：0.069
- Grocery：0.063
- Toys：0.028
- Movies：0.022
- Reddit-S：0

整体也确实与 OFR−OADD 的强弱关系较一致。

所以我认可：

∥Ifk∥F/∥W0∥F\boxed{ \|I_{fk}\|_F/\|W_0\|_F }

以后应该替代：

∥Cfk∥F/∥W0∥F\|C_{fk}\|_F/\|W_0\|_F

作为论文里的 interaction mechanism diagnostic。

------

# 二、但 interaction strength 最好再加一个 normalized 版本

你目前定义：

SI=∑f,kufk∥Ifk∥F∥W0∥F.S_I= \sum_{f,k} u_{fk} \frac{\|I_{fk}\|_F}{\|W_0\|_F}.

这里：

ufk=1N∑iΓifk.u_{fk}=\frac1N\sum_i\Gamma_{ifk}.

这个量有一个小问题：

∑fkufk\sum_{fk}u_{fk}

不是恒定的 1，它受总 graph mass 影响。

因此跨数据集比较：

> ele-fashion 0.069 > Grocery 0.063

既包含 interaction magnitude，也包含 graph usage 总量。

建议无需重训，再同时输出：

SˉI=∑fkufk∥Ifk∥F(∑fkufk)∥W0∥F+ϵ\boxed{ \bar S_I= \frac{ \sum_{fk}u_{fk}\|I_{fk}\|_F }{ (\sum_{fk}u_{fk})\|W_0\|_F+\epsilon } }

即 usage-weighted **average** interaction strength。

原来的 raw SIS_I 可以保留，两者一起报告：

- raw：实际总体 interaction exposure；
- normalized：使用 graph 时平均 interaction 强度。

这是小修，不影响现有结论。

------

# 三、关于 Reddit-S，要继续保持“相关”而不是“因果”

你现在写：

> Reddit-S 的 Γ\Gamma uniform，因此 effective interaction 为 0。

建议仍保持：

> **与这一机制一致**

而不要写成严格因果：

Γ uniform⇒Ifk=0.\Gamma\text{ uniform}\Rightarrow I_{fk}=0.

因为数学上 uniform Γ\Gamma 本身并不会强制训练后的：

TfkT_{fk}

相同或 additive。

更准确是：

> 当 factor-specific relation allocation 退化时，cell interaction 对最终消息的可利用性大幅下降，因此训练没有形成可识别的 effective interaction。

目前报告的措辞“与……一致”基本是安全的。

------

# 五、最重要的数学问题：B16 实际上已经有能力表示任意 12 个 cell matrices

你的 Basis：

Dfk=∑b=1BcfkbVb.D_{fk} = \sum_{b=1}^{B} c_{fkb}V_b.

把每个：

d×dd\times d

矩阵 flatten 成长度：

d2d^2

的向量。

因为：

F×K=3×4=12,F\times K=3\times4=12,

整个 residual operator family 可以写成：

D∈R12×d2.D \in\mathbb R^{12\times d^2}.

Basis parameterization 就是：

D=CV\boxed{ D=CV }

其中：

C∈R12×B,V∈RB×d2.C\in\mathbb R^{12\times B}, \qquad V\in\mathbb R^{B\times d^2}.

所以只要：

B≥12\boxed{B\ge12}

理论上它就已经可以表示任意：

D∈R12×d2.D\in\mathbb R^{12\times d^2}.

例如直接取：

C=[I12,0]C=[I_{12},0]

然后：

V1,…,V12V_1,\ldots,V_{12}

分别等于 12 个目标 cell residual matrices 即可。

因此：

Basis16 并不存在 cell-operator function-class expressivity 限制。\boxed{ \textbf{Basis16 并不存在 cell-operator function-class expressivity 限制。} }

这一点非常关键。

------

# 六、所以报告中这句话目前不成立

现在报告写：

> shared basis 无论 low-rank 还是 full-matrix，都只能把 cell 差异限制在共享子空间内，因此需要 free per-cell matrices。

对于：

B=4, 8B=4,\ 8

这句话成立。

但对于：

B=16>12\boxed{B=16>12}

就不成立了。

因为最多只有 12 个 cell。

B16 的 basis subspace：

span⁡(V1,…,V16)\operatorname{span}(V_1,\ldots,V_{16})

完全可以包含所有 12 个任意 cell residual。

因此你观察到：

Basis16≪OFRBasis16\ll OFR

不能再解释成：

representation capacity 不够\boxed{\text{representation capacity 不够}}

而更可能是：

parameterization / optimization / implicit regularization 问题。\boxed{ \textbf{parameterization / optimization / implicit regularization 问题。} }

这会直接改变 P3 capacity analysis 的措辞。

------

# 七、代码中还确实存在一个 Basis 初始化问题

当前代码是：

```python
self.V = nn.Parameter(
    torch.empty(B, d, d)
)
nn.init.xavier_uniform_(self.V)
```

。

如果你的设计意图是：

> 每个 VbV_b 都是独立的 full-matrix Xavier initialization

那么这里实际上**没有做到**。

PyTorch 对三维 tensor：

```text
[B,d,d]
```

调用 Xavier 时，不会把它理解为：

> B 个独立 `[d,d]` Linear matrices。

而会按照高维 weight tensor 的 fan 规则计算整体 fan-in/fan-out。

对：

d=128d=128

来说，单个 `[128,128]` matrix 的 Xavier uniform bound 大约：

6128+128≈0.153.\sqrt{\frac6{128+128}} \approx0.153.

但当前 `[B,128,128]` 整体 Xavier 的 scale 大约只有：

0.0180.018

左右。

也就是小了大约：

8×\boxed{8\times}

量级。

------

# 八、而你的 `c=0` 初始化会进一步放大这个问题

Basis 初始化：

cfkb=0.c_{fkb}=0.

所以 step 0：

Dfk=0D_{fk}=0

这是对的，可以保证：

Tfk=W0.T_{fk}=W_0.

但是梯度传播：

第一步：

∇VbL∝cfkb=0.\nabla_{V_b}L \propto c_{fkb} = 0.

因此：

Vb 第一步没有 residual gradient。\boxed{ V_b\text{ 第一步没有 residual gradient。} }

而：

∇cfkbL\nabla_{c_{fkb}}L

又与：

VbV_b

的大小直接相关。

现在 VbV_b 又因为 3-D Xavier 被初始化得大约小 8 倍，于是：

cc

的 bootstrap gradient 也明显偏小。

接下来：

VV

只有等 cc 长起来以后才开始真正学习。

这与你的观察高度吻合：

Basis16 的 message deviation 只有：

0.22∼0.430.22\sim0.43

左右，

而 OFR：

0.90∼0.99.0.90\sim0.99.



所以目前 Basis 学不到 OFR 并不能简单解释为：

> basis structure 不行。

其中存在很现实的 optimization disadvantage。

------

# 九、Basis 初始化应该改成逐 basis Xavier

应该是：

```python
self.V = nn.Parameter(
    torch.empty(B, d, d)
)

for b in range(B):
    nn.init.xavier_uniform_(self.V[b])
```

这样：

VbV_b

才真正按照独立：

d×dd\times d

linear transformation 初始化。

同时保留：

c=0c=0

即可继续满足：

Tfk(0)=W0.T_{fk}^{(0)}=W_0.

这个修改不会破坏你的 zero-init discipline。

------

# 十、更重要的是：现在其实出现了一个比 Basis 更干净的实验

我建议增加：

Direct Cell Operator\boxed{ \textbf{Direct Cell Operator} }

定义非常简单：

Tfk=W0+Dfk\boxed{ T_{fk} = W_0+D_{fk} }

其中：

D∈RF×K×d×dD\in\mathbb R^{F\times K\times d\times d}

直接 zero init。

没有：

Af, Bk, Cfk.A_f,\ B_k,\ C_{fk}.

------

# 十一、为什么这个实验非常重要？

因为当前 OFR：

Tfk=W0+Af+Bk+Cfk.T_{fk} = W_0+A_f+B_k+C_{fk}.

由于：

CfkC_{fk}

本身就是完全自由矩阵，所以定义：

Dfk=Af+Bk+Cfk.D_{fk}=A_f+B_k+C_{fk}.

立即得到：

Tfk=W0+Dfk.\boxed{ T_{fk}=W_0+D_{fk}. }

反过来，对于任意 DirectCell：

Dfk,D_{fk},

都可以令：

Af=0,Bk=0,Cfk=Dfk.A_f=0,\quad B_k=0,\quad C_{fk}=D_{fk}.

因此：

DirectCell 与 OFR 的有效函数类完全相同。\boxed{ \textbf{DirectCell 与 OFR 的有效函数类完全相同。} }

这是一个非常强的数学事实。

------

# 十二、但 DirectCell 参数更少

OFR 当前额外参数：

(F+K+FK)d2(F+K+FK)d^2

即：

19×1282=311,296.19\times128^2 = 311,296.

DirectCell：

FKd2=12×1282=196,608.FKd^2 = 12\times128^2 = 196,608.

减少：

114,688114,688

约：

36.8%\boxed{36.8\%}

的 residual parameters。

而 function class **没有任何损失**。

所以这其实是当前最应该测试的 P3-C。

------

# 十三、这个实验还可以彻底解释 OFR 为什么有效

最终只有两种结果。

### Case A

DirectCell≈OFR.DirectCell\approx OFR.

那么说明：

真正需要的是 unrestricted cell-conditioned transformation，\boxed{ \text{真正需要的是 unrestricted cell-conditioned transformation，} }

而：

A+B+CA+B+C

只是冗余 parameterization。

此时 final model 应直接换成：

Tfk=W0+Dfk.\boxed{ T_{fk}=W_0+D_{fk}. }

这样：

- 更干净；
- 参数更少；
- 无 A/B/C 不可辨识；
- interaction diagnostic 仍可直接 double-center TT；
- 论文故事更简单。

这会是非常好的结果。

------

### Case B

OFR>DirectCell.OFR>DirectCell.

注意这时**不能说 OFR function class 更强**。

因为两者 function class 相同。

只能说明：

\boxed{ \textbf{ A_f+B_k+C_{fk} \text{ 的 overparameterized decomposition 带来了优化/implicit-bias 优势。} }

这同样是很有价值的实验结论。

------

# 十四、这也解释为什么当前 Basis16 失败值得重新审视

从函数类角度：

Basis16Basis16

其实也能够表示任意 12 个 cell matrices。

但它是双线性 factorization：

D=CV.D=CV.

而 DirectCell：

DD

本身直接优化。

所以：

Basis16≪OFRBasis16\ll OFR

真正提示的可能是：

direct optimization of cell matrices is easier than bilinear factorized optimization.\boxed{ \textbf{direct optimization of cell matrices is easier than bilinear factorized optimization.} }

而不是：

> shared basis 无法表示 cell differences。

这两个故事差别很大。

------

# 十五、因此我建议现在把 Supplement 中的 capacity 结论暂时改掉

不要写：

> transformation specialization 需要 free per-cell matrices，shared basis 无法压缩。

暂时改成：

> **The gains of cell-conditioned transformation are not preserved by the tested low-rank or basis-factorized parameterizations. In particular, increasing the number of full-matrix bases does not produce monotonic performance recovery, suggesting that parameterization and optimization, rather than raw parameter count alone, are critical.**

中文：

> **当前低秩和 basis-factorized 参数化均未能稳定保留 Full Cell-conditioned Operator 的收益；增加 full-matrix basis 数量也未产生单调性能恢复，说明该现象不能由原始参数量简单解释，parameterization 与 optimization 可能发挥关键作用。**

等 DirectCell 跑完，再决定能不能升级成：

> free per-cell matrix freedom is necessary.

------

# 十八、最终复现入口已经修正确了

现在：

```
biaxis_final.yaml
```

明确冻结：

```yaml
p2:
  mode: null_softmax
  deterministic: false

p3:
  operator_mode: full_interaction
```

并且 `biaxis_final.py` 是一个非常薄的 alias，只复用 P3 model。这个设计是正确的。

不过还有一个非常小的清理项：

当前 final config 里仍有：

```yaml
lowrank_rank: 16
basis_num_bases: 8
```

但：

operator_mode=full_interactionoperator\_mode=full\_interaction

时二者完全不会被使用。

既然文件顶部写了：

> NEVER add experiment knobs here

建议把这两个删掉。

`biaxis_p3.py` 本身已经给 `lowrank_rank` 和 `basis_num_bases` 提供默认值，所以删掉不会影响 OFR。

这样 final config 会更干净。

------

# 十九、现在需要补多少实验？

我不会再让你做大规模 sweep。

我建议最后只做：

### 1. 修 Basis 初始化

改成 per-basis Xavier。

### 2. DirectCell

实现：

Tfk=W0+Dfk.T_{fk}=W_0+D_{fk}.

### 3. 第一轮

只跑：

5 datasets×2 variants×3 seeds=305\ datasets \times 2\ variants \times 3\ seeds = 30

其中：

- corrected Basis16
- DirectCell

O0 / OADD / OFR 全部已有。

------

如果想再省：

DirectCell 优先级明显高于 corrected Basis16。

最小可以只跑：

5×1×3=15\boxed{ 5\times1\times3=15 }

个 DirectCell runs。

如果 DirectCell 已经≈OFR，Basis 根本没必要继续救。

------

# 二十、我预期最值得期待的其实是 DirectCell

因为它同时满足：

same effective function class as OFR\boxed{ \text{same effective function class as OFR} }

- 

37% fewer residual parameters\boxed{ 37\%\text{ fewer residual parameters} }

- 

no A/B/C decomposition ambiguity\boxed{ \text{no A/B/C decomposition ambiguity} }

- 

direct zero-init optimization\boxed{ \text{direct zero-init optimization} }

如果它保留 OFR 的增益，那你的最终模型会变得比现在更漂亮：

mif=∑kΓifk(W0+Dfk)gikf\boxed{ m_i^f = \sum_k \Gamma_{ifk} (W_0+D_{fk})g_{ik}^f }

甚至论文都不需要再解释：

A/B/CA/B/C

是什么。

OADD / OF / OR 只作为科学消融存在。